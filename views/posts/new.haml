/change this to be post/new and use restful routing in the app file.

%h1 Y'all are fixin' to jot somethin' down!

%form{action: "/users/#{@user.id}/posts/new", method: 'POST'}
	-#why is it .id here, if we already defined @user as being User.find(parems[:id])?
	
	%label{for: '[post]title'} Title:
	%br
	%input{type: 'text', name: '[post]title'}
	%br
	%br
	%label{for: '[post]body'} Spit it out:
	%br
	%textarea{name: '[post]body'}
	%br
	%input.btn{type: 'submit', value: 'post that pony'}

